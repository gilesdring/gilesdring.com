<p>
Published on <time class="dt-published" datetime="{{ post.data.date | date('ATOM') }}">{{ post.data.date | date('HUMAN_DATE') }}</time>.
</p>

{# {% if post.tags.size > 0 %}<ul class='tag-cloud'>
  {% for tag in post.tags %}<li>
    <a href=''>{{tag}}</a>
  </li>{% endfor %}
</ul>{% endif %} #}
